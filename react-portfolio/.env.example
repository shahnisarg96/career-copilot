# React Frontend
# When running the frontend via the nginx Docker container, use nginx's proxy:
VITE_API_BASE=/api

# Alternative for running the frontend directly on your host (no nginx proxy):
# VITE_API_BASE=http://localhost:8080
REACT_PORT=5173
FRONTEND_URL=http://localhost:5173
